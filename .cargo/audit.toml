# .cargo/audit.toml
[advisories]
# See issue #5: Track cargo audit vulnerabilities (transitive dependencies)
ignore = [
    "RUSTSEC-2023-0071",  # RSA Marvin Attack via sqlx-mysql (we use Postgres only)
    "RUSTSEC-2025-0134",  # rustls-pemfile unmaintained via oauth2 crate
]
